{
  "version": 1,
  "agents": [
    {
      "name": "프로젝트_스캔만",
      "description": "HVDC PJT와 cursor-mcp 스캔 및 규칙 적용만 실행 (안전 검증)",
      "workingDirectory": "${workspaceRoot}",
      "env": { "PYTHONUTF8": "1" },
      "steps": [
        { "run": "python devmind.py scan --paths \"C:\\\\HVDC PJT\" --paths \"C:\\\\cursor-mcp\" --emit .cache/scan.json --safe-map .cache/safe_map.json --sample-bytes 4096" },
        { "run": "python devmind.py rules --scan .cache/scan.json --emit .cache/scores.json" },
        { "openFile": ".cache/scan.json" }
      ],
      "timeout": 0
    },
    {
      "name": "프로젝트_정리실행",
      "description": "스캔 완료 후 프로젝트 클러스터링, 정리, 리포트 생성",
      "workingDirectory": "${workspaceRoot}",
      "env": { 
        "PYTHONUTF8": "1",
        "OPENAI_API_KEY": "${env:OPENAI_API_KEY}"
      },
      "steps": [
        { "run": "python devmind.py cluster --scores .cache/scores.json --emit .cache/projects.json --project-mode local" },
        { "run": "python devmind.py organize --projects .cache/projects.json --scores .cache/scores.json --target \"C:\\\\PROJECTS_STRUCT\" --mode move --conflict version --journal .cache/journal.jsonl --schema schema.yml" },
        { "run": "python devmind.py report --journal .cache/journal.jsonl --out reports/projects_summary.html" },
        { "openFile": "reports/projects_summary.html" }
      ],
      "timeout": 0
    },
    {
      "name": "프로젝트_GPT모드",
      "description": "GPT 모드로 프로젝트 클러스터링 (API 키 필요)",
      "workingDirectory": "${workspaceRoot}",
      "env": { 
        "PYTHONUTF8": "1",
        "OPENAI_API_KEY": "${env:OPENAI_API_KEY}"
      },
      "steps": [
        { "run": "python devmind.py cluster --scores .cache/scores.json --emit .cache/projects.json --project-mode gpt --safe-map .cache/safe_map.json" },
        { "run": "python devmind.py organize --projects .cache/projects.json --scores .cache/scores.json --target \"C:\\\\PROJECTS_STRUCT\" --mode move --conflict version --journal .cache/journal.jsonl --schema schema.yml" },
        { "run": "python devmind.py report --journal .cache/journal.jsonl --out reports/projects_summary.html" },
        { "openFile": "reports/projects_summary.html" }
      ],
      "timeout": 0
    }
  ]
}