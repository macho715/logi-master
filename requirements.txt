# MACHO-GPT 핵심 의존성
click>=8.0.0
blake3>=0.3.0
scikit-learn>=1.0.0
pandas>=1.3.0
numpy>=1.21.0
pydantic>=2.6.0

# 웹 인터페이스
streamlit>=1.28.0

# 설정 파일 처리
pyyaml>=6.0
toml>=0.10.0

# 선택적 의존성 (개발/테스트용)
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0
pytest-benchmark>=4.0.0

# 코드 품질
flake8>=5.0.0
mypy>=1.0.0
black>=22.0.0
isort>=5.10.0

# 보안
bandit>=1.7.0
safety>=2.0.0

# 성능 모니터링
memory-profiler>=0.60.0
psutil>=5.9.0

# 선택적 AI/ML 의존성
openai>=1.0.0  # GPT 모드용
sentence-transformers>=2.2.0  # 로컬 임베딩용
faiss-cpu>=1.7.0  # 벡터 검색용

# 개발 도구
rich>=13.0.0  # CLI 스타일링
